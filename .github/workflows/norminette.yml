name: Norminette

on:
  push:
    branches: [ main, master, dev, doriane, killian ]
  pull_request:
    branches: [ main, master, dev, doriane, killian ]

jobs:
  norminette:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Run norminette
        uses: alexandregv/norminette-action@v3